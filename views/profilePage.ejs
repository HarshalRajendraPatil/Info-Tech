<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="shortcut icon" href="img/favicon.ico" type="image/x-icon" />
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Gabarito:wght@500&display=swap");
      @import url("https://fonts.googleapis.com/css2?family=Gabarito:wght@500&family=Roboto+Condensed&display=swap");
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      html {
        font-size: 62.5%;
      }

      body {
        background: linear-gradient(to right, #ffb6c1, #87ceeb);
        font-family: "Gabarito", sans-serif;
      }

      h1 {
        font-size: 3rem;
        color: rgb(200, 108, 96);
      }

      h1 span {
        color: rgb(91, 4, 109);
      }

      .header {
        display: flex;
        gap: 1rem;
        justify-content: space-between;
        align-items: center;
        background-color: black;
        padding: 1.6rem 3.2rem;
      }

      .logo {
        height: 5.5rem;
      }

      .nav-link-box {
        display: flex;
        list-style: none;
        gap: 4rem;
        font-family: "Gabarito", sans-serif;
      }

      .nav-link:link,
      .nav-link:visited {
        display: inline-block;
        text-decoration: none;
        font-size: 2rem;
        color: #fff;
        transition: all 0.3s;
      }

      .nav-link:hover,
      .nav-link:active {
        transform: scale(1.4);
        color: greenyellow;
      }

      .container {
        max-width: 120rem;
        display: flex;
        flex-direction: column;
        gap: 5rem;
        padding: 1.5rem 3rem;
        margin: 0 auto;
      }

      .blog-info {
        display: flex;
        gap: 4rem;
        background-color: rgba(255, 255, 255, 0.32);
        color: black;
        border: 3px solid white;
        padding: 1.5rem 3rem;
        border-radius: 10px;
        font-family: "Gabarito", sans-serif;
        transition: all 0.3s linear;
      }

      .blog-info:hover {
        transform: scale(1.05);
      }

      .blog-img {
        width: 30%;
      }

      .blog-text {
        display: flex;
        flex-direction: column;
        gap: 3rem;
        justify-content: center;
        font-family: "Gabarito", sans-serif;
      }

      .blog-title {
        font-size: 5rem;
      }

      .blog-body {
        font-size: 2rem;
      }

      .link:link,
      .link:visited {
        color: white;
        font-weight: bolder;
      }

      .img {
        width: 100%;
        height: 100%;
      }

      .search-box {
        max-width: 40%;
        display: flex;
        align-items: center;
        gap: 1rem;
      }

      .search {
        width: 80%;
        padding: 0.5rem 1rem;
        font-size: 2rem;
        border-radius: 10px;
        color: rgb(0, 30, 255);
        font-weight: bold;
        transition: all 0.3s linear;
      }

      .search:hover {
        transform: scale(1.06);
      }

      .submit {
        padding: 0.5rem;
        font-size: 2rem;
        border-radius: 10px;
        color: white;
        background-color: #2691d9;
      }

      .submit:hover {
        transform: scale(1.1);
        background: black;
        border-radius: 8px;
      }

      .search-box-2 {
        display: none;
      }

      .readBy {
        font-size: 1.5rem;
        color: white;
      }

      .readBy span {
        color: black;
      }

      @media (max-width: 900px) {
        .blog-info {
          padding: 1rem 1.5rem;
        }
      }

      @media (max-width: 850px) {
        html {
          font-size: 50%;
        }
        .nav-link-box {
          gap: 2rem;
        }
      }

      @media (max-width: 700px) {
        .blog-info {
          flex-direction: column;
        }
        .blog-img {
          display: block;
          width: 100%;
          margin: 0 auto;
        }
        .blog-title {
          text-align: center;
        }
      }

      @media (max-width: 600px) {
        html {
          font-size: 45%;
        }
        .header {
          padding: 1rem;
        }
      }

      @media (max-width: 500px) {
        .blog-img {
          width: 100%;
        }
        .search {
          font-size: 1rem;
        }
        .submit {
          font-size: 1rem;
        }
      }

      @media (max-width: 450px) {
        html {
          font-size: 35%;
        }
      }

      @media (max-width: 400px) {
        .header {
          padding: 1rem 1rem 0 1rem;
          gap: 1rem;
        }
      }
    </style>
    <title>Profile Page</title>
  </head>
  <body>
    <header class="header">
      <div class="header-img">
        <img src="img/logo.png" class="logo" alt="" srcset="" />
      </div>
      <nav class="nav">
        <ul class="nav-link-box">
          <li><a href="/" class="nav-link">Home</a></li>
          <li><a href="/blogs" class="nav-link">Blogs</a></li>
          <li><a href="/subscribe" class="nav-link">Subscribe</a></li>
          <li><a href="/profile" class="nav-link">Profile</a></li>
          <li><a href="/logout" class="nav-link">Logout</a></li>
        </ul>
      </nav>
    </header>
    <div class="container">
      <h1>Name: <span><%= name %></span></h1>
      <h1>Email: <span><%= email %></span></h1>
      <h1>Joined Us on: <span><%= joined %></span></h1>
      <h1>Is subscribed: <span><%= isSubs %></span></h1>
      <h1>Blogs posted: <span><%= numBlog %></span></h1>
      <% blog.forEach(el => { %>
      <div class="blog-info">
        <div class="blog-img">
          <img
            src="<%= el.coverImage %>"
            class="img"
            alt="Image related to blog"
            srcset=""
          />
        </div>
        <div class="blog-text">
          <h2 class="blog-title"><%= el.title %></h2>
          <p class="blog-body">
            <%= el.body %>...
            <a href="/blogs/<%= el._id %>" class="link">Read More</a>
          </p>
          <p class="readBy">Read by <span><%= el.readBy %></span> people</p>
        </div>
      </div>
      <% }) %>
    </div>
  </body>
</html>
